apiVersion: v1
kind: Namespace
metadata:
  name: castai-workload-resize-migrator
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: castai-workload-resize-migrator
  namespace: castai-workload-resize-migrator
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: castai-workload-resize-migrator
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["pods/status"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "create", "update"]
    resourceNames: ["castai-workload-resize-migrator"]
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "create", "update"]
    resourceNames: ["castai-workload-resize-migrator"]
  - apiGroups: ["live.cast.ai"]
    resources: ["migrations"]
    verbs: ["get", "list", "watch", "create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: castai-workload-resize-migrator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: castai-workload-resize-migrator
subjects:
  - kind: ServiceAccount
    name: castai-workload-resize-migrator
    namespace: castai-workload-resize-migrator
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: castai-workload-resize-migrator
  namespace: castai-workload-resize-migrator
data:
  placeholder: "leader-election-lock"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: castai-workload-resize-migrator
  namespace: castai-workload-resize-migrator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: castai-workload-resize-migrator
  template:
    metadata:
      labels:
        app: castai-workload-resize-migrator
    spec:
      serviceAccountName: castai-workload-resize-migrator
      containers:
        - name: controller
          image: ttl.sh/castai-workload-resize-migrator:latest
          imagePullPolicy: Always
          env:
            - name: DRY_RUN
              value: "true"
            - name: PENDING_THRESHOLD
              value: "2m"
            - name: MIGRATION_TIMEOUT
              value: "10m"
            - name: SAFETY_SCAN_STARTUP_DELAY
              value: "10s"
            - name: MIGRATION_CLEANUP_INTERVAL
              value: "30s"
            - name: MIGRATION_RETRY_LIMIT
              value: "3"
            - name: CLM_NODE_TEMPLATE
              value: "clm-live-migration-template"
            - name: LEADER_ELECTION_LEASE_NAME
              value: "castai-workload-resize-migrator"
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              memory: "256Mi"
